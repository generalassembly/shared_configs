#!/usr/bin/env -S bash -e

if [[ -z $BUNDLE_PATH || -z $GEMS_CACHE_KEY ]]; then
  echo "ERROR: BUNDLE_PATH and GEMS_CACHE_KEY must be exported in .semaphore/bin/setup-repo"
fi

cache store $GEMS_CACHE_KEY $BUNDLE_PATH
